package GUI;

import javax.swing.*;
import java.awt.*;

public class PlanesRenderingUtils {

    /**
     *
     * 创建一个指定相对位置,和大小的面板
     * @param container 指定容器
     * @param hight 容器指定高度
     * @param width 容器指定宽度
     * @param x  容器指定相对坐标X
     * @param y  容器指定相对坐标y
     * @param backGrand 背景色
     */
    public void setRelativelyPositionPlane(Container container, int x, int y, int width, int hight,Color backGrand,
                                           Class<? extends JComponent>  plane){
        if(plane.getName().equals("JPanel")){

        }
    }
}
